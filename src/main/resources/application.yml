spring:
  data:
    mongodb:
      database: library
management:
  endpoints:
    web:
      exposure:
        include:
          - beans
          - health
          - metrics
          - env
          - logfile
          - hystrix.stream
logging:
  path: /
  file: application.log
